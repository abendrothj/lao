[package]
name = "gguf_plugin"
version = "0.1.20"
edition = "2021"
description = "Offline LLM via GGUF"
authors = ["Jake Abendroth <contact@jakea.net>"]

[lib]
crate-type = ["cdylib"]

[dependencies]
lao_plugin_api = { path = "../../lao_plugin_api" } 